//break and continue


//skips when x is equal to 0 with continue
for(let x=0;x<=10;x++){
    if(x==0)
        continue;
    console.log(x);
}

//skips when x is equal to 0 with break
for(let x=10;x>=0;x--){
    if(x==0)    
        break;
    console.log(x);
}